import { BrainContext } from "../types";

export function decideFeatures(context: BrainContext) {
  // Minimal stub: return a neutral decision object used by callers.
  return {
    ok: true,
    code: "OK",
    meta: { text: "features placeholder" },
  };
}

export default decideFeatures;
